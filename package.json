{
  "name": "game-interaction",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TPS Loot System prototype — dev server wrapper around the standalone HTML file",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
